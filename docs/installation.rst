============
Installation
============

At the command line::

    $ pip install dj-libcloud

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-libcloud
    $ pip install dj-libcloud